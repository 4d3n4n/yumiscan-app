# v1.5.3 — Foreground Resume UX

Date: 2026-03-16

## Scope

Checklist officielle de la passe `1.5.3`, centree sur la reprise de l'app apres retour au premier plan.

Objectifs:

- reduire la latence percue au retour sur `/`
- reduire la latence percue au retour sur `/app/dashboard`
- reduire la latence percue au retour sur `/app/scan/[id]`
- reduire la latence percue au retour sur `/app/account`
- garder le contenu precedent visible pendant la revalidation quand c'est possible
- dedoublonner les refreshs foreground redondants

## Checklist

- [x] Home: supprimer les refetchs pricing foreground redondants au simple remount
- [x] Dashboard: supprimer le refetch manuel doublon au mount
- [x] Dashboard: garder la liste visible pendant la revalidation foreground si des scans existent deja
- [x] Dashboard: ajouter un gate de cooldown sur `visibilitychange` / `pageshow`
- [x] Account: ne plus cacher toute la page si un profil precedent existe deja
- [x] Account: dedoublonner les refreshs auth foreground
- [x] Scan detail: ne plus repasser en skeleton plein ecran si le scan est deja disponible
- [x] Validation: tests perf/runtime ciblés verts

## Guardrails

- ne pas regressser la coherence auth
- ne pas reintroduire de signed URL persistante en `localStorage`
- ne pas exposer de donnees user en cas d'etat auth `unauthenticated`
- conserver les invalidations metier existantes

## Extension pricing

Dans la continuite de cette passe perf, le pricing a ete sorti en route canonique `/pricing`, tout en gardant un bloc resume sur la home.

Objectifs associes:

- structure plus propre entre acquisition home et conversion pricing
- navigation `Tarifs` plus stable qu'une ancre `#pricing`
- cache pricing plus canonique
- FAQ achat disponible sur la home et sur la page dediee
- clarification des fonctionnalites `Scan IA` et `Assistant IA`
- integration d'une FAQ sur les moyens de paiement acceptes

Reference d'architecture:

- [pricing-page-runtime-rationale-2026-03-16.md](/Users/adenankhachnane/Downloads/projet-full-stack/main/docs/performance/pricing-page-runtime-rationale-2026-03-16.md)

Point de vigilance ferme dans cette passe:

- suppression de l'ancienne `routeRule` Nuxt qui redirigeait encore `/pricing` vers `/#pricing`
- chargement runtime des fichiers `pricing.json` dans la config i18n, pas seulement dans les tests
- remplacement de la carte trust Stripe trop abstraite sur la home par un resume paiement plus parlant avec logos reels
- mutualisation du carousel de paiements entre home et `/pricing`
- ajout d'un rappel visuel Stripe discret dans le footer
- home: carousel paiements compact non interactif pour rester lisible et plus leger que sur `/pricing`
- home: carousel reviews recadre pour que chaque avis soit une vraie card centree avec son propre espacement
