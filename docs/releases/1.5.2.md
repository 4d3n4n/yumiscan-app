# v1.5.2 — Security Hardening Follow-up

Date: 2026-03-15

## Scope

Checklist officielle de la passe `1.5.2`, issue du dernier audit securite.

Objectifs:
- rendre les suppressions de compte fail-closed
- durcir la CSP sans casser le rendu actuel
- retirer la persistance locale des signed URLs privees
- garder une UX propre pendant le rechargement d'image
- sortir les tables internes Sentry de `public`
- formaliser l'ownership des zones sensibles
- eviter les prix stale apres edition back-office et fiabiliser le checkout Stripe embed

## Checklist

- [x] Point 1: suppression de compte fail-closed cote `user-account-delete`
- [x] Point 1: suppression de compte fail-closed cote `admin-delete-user`
- [x] Point 3: analyse CSP faite, mais retrait global de `unsafe-inline` reporte car Nuxt casse sans nonce/hash
- [x] Point 4: retirer `localStorage` du cache des signed URLs
- [x] Point 4: garder un loading image propre sur dashboard et detail scan
- [x] Point 5: deplacer les tables Sentry backend-only hors de `public`
- [x] Point 5: rerouter `sentry-webhook` via une surface d'acces backend dediee
- [x] Point 6: ajouter un `CODEOWNERS` minimal sur les zones sensibles
- [x] Pricing: resynchroniser le cache public apres save back-office
- [x] Pricing: detruire tout checkout Stripe embed precedent avant remount
- [x] Pricing: revalider legerement la home au retour de page sans polling continu
- [x] Validation: tests securite/caches/CSP verts

## Guardrails

- ne pas toucher au point `verify_jwt` de la gateway dans cette passe
- ne pas regresser l'UX dashboard/scan pendant la resignation des images
- garder la compatibilite avec les migrations et les tests existants

## Notes

- `style-src 'unsafe-inline'` reste conserve tant que le front depend encore d'attributs inline multiples
- `script-src 'unsafe-inline'` a ete reintroduit: Nuxt injecte encore du bootstrap/payload inline, donc retirer ce flag casse le montage client
- le vrai hardening CSP restant demandera une passe dediee nonce/hash et pas une suppression brute

## Validation

- `npm test` passe: `83` fichiers, `275` tests verts
- `npm run lint:deadcode` passe
- `npx vue-tsc --noEmit --pretty false` passe
- `npm run typecheck` passe
- `deno check` passe sur les Edge Functions touchees
